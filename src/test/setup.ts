/**
 * Vitest Test Setup
 *
 * This file is loaded before all tests to configure the testing environment.
 * It includes global test utilities and DOM matchers from @testing-library/jest-dom.
 */

import '@testing-library/jest-dom';

// Optional: Add custom matchers or global test configuration here
// Example: global.fetch = vi.fn();
